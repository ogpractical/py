*slip 26*

Q1. Write a python program to remove all whitespaces from a given string. Display modified string

str1 = input("Enter a string: ")
modified = str1.replace(" ", "")
print("Modified String:", modified)


Q2 Create a package vehicle with a sub package types, sub package type contains modules cars and bikes. Each module should have a function available_models() returning a list of models. Write a python program to display all available models of cars and bikes.

# cars.py
def available_models():
    return ["Swift", "Baleno", "Creta", "Fortuner"]

# bikes.py
def available_models():
    return ["Splendor", "Pulsar", "Activa", "Apache"]

# main.py
from vehicle.types import cars, bikes

print("Available Car Models:")
for car in cars.available_models():
    print(car)

print("\nAvailable Bike Models:")
for bike in bikes.available_models():
    print(bike)


Or


Q2. Create a following DataFrame named as “data”. Write the python code for the following commands. 

import pandas as pd
data = pd.DataFrame({
    'Company': ['Apsara', 'Natraj', 'Cello', 'Parkar', 'Apsara'],
    'Count': [15, 20, 25, 35, 20],
    'Price': [250, 200, 600, 900, 300]
}, index=['Pencil', 'Pencil', 'Pen', 'Pen', 'Eraser'])
print("Original DataFrame:")
print(data)



#a) Find all rows with the label “Pencil”. Extract all columns 

print("\nRows with label Pencil:")
print(data.loc['Pencil'])


b)Change the Eraser count as 25 instead of 20.

data.loc['Eraser', 'Count'] = 25
print("\nAfter changing Eraser count:")
print(data)


#c) List only the columns Company and Price. 

print("\nCompany and Price columns:")
print(data[['Company', 'Price']])


#d) List only rows with labels ‘Pencil’ and ‘Pen’

print("\nRows with labels Pencil and Pen:")
print(data.loc[['Pencil', 'Pen']]).


e) Add a new column named Colour to the DataFrame and display DataFrame.. 

data['Colour'] = ['Black', 'Red', 'Blue', 'Green', 'White']
print("\nDataFrame after adding Colour column:")
print(data)
